# .github/workflows/ci.yml
# Copy this file to your repo's .github/workflows/ directory.
# This is identical in every NextNode repo — no modifications needed.
name: CI

on:
    push:
        branches: [main]
    pull_request:
    workflow_dispatch:

permissions:
    contents: write
    issues: write
    pull-requests: write

jobs:
    pipeline:
        uses: NextNodeSolutions/infrastructure/.github/workflows/pipeline.yml@main
        permissions:
            contents: write
            issues: write
            pull-requests: write
        secrets: inherit
